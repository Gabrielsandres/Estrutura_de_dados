/*Implemente uma versão do Selection Sort que conta o número de comparações e trocas realizadas durante a ordenação. 
Execute seu algoritmo em diferentes tamanhos de entrada e compare os números de comparações e trocas para entender como 
o desempenho varia.*/

#include <stdio.h>
#include <stdlib.h> // Para utilizar a função rand()
#include <time.h>   // Para inicializar a semente da função rand() com o tempo atual

#define tamanho 1000

int main(){
    srand(time(NULL));

    for ( i = 0; i < count; i++)
    {
        /* code */
    }
    
}